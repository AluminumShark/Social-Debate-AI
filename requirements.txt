# Runtime dependencies (kept in sync with pyproject.toml).
# Preferred install: `uv sync`  or  `pip install -e .`

# Deep learning
torch>=2.0.0
torch-geometric>=2.5.0
numpy>=1.24.0
scipy>=1.10.0

# Text encoders / dataset (training)
transformers>=4.40.0
convokit>=3.0.0

# LangChain / LangGraph
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-core>=0.3.0
langchain-community>=0.4.1
langgraph>=0.2.0

# Vector search
faiss-cpu>=1.7.0

# API / web
openai>=1.0.0
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.0.0
python-dotenv>=1.0.0

# Config / utils
PyYAML>=6.0
tqdm>=4.65.0
networkx>=3.0
tiktoken>=0.5.0
aiohttp>=3.8.0
